#name: Deploy via ftp
#on:
 # push:
  #  branches: master
#jobs:
#  deploy:
#    name: Deploy
#    runs-on: ubuntu-latest
#    steps:
#    - name: 🚚 Get latest code
#      uses: actions/checkout@v2
#    
#    - name: 📂 Sync files
#      uses: SamKirkland/FTP-Deploy-Action@4.1.0
#      with:
#        server: clarus.timeweb.ru
#        username: cu87343_main
#        password: ${{ secrets.FTP_PASSWORD }}
#        server-dir: /setin/public_html/
